# packages/contracts

Future source of versioned command, event, tool, artifact, logging, and RunSpec schemas plus cross-language fixtures. Contracts are described in `docs/04_COMMAND_EVENT_CONTRACTS.md`; no package is implemented yet.
