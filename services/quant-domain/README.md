# services/quant-domain

Future Python service owning durable domain writes, SQLite WAL migrations, Activity/Task/Context/Revision/Variant/Run state, inbox/outbox, Job Runner, logging lifecycle, artifact metadata, and project import/export. No service is implemented yet.
