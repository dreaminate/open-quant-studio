[project]
name = "open-quant-studio-quant-domain"
version = "0.0.0"
description = "Python durable-domain boundary for Open Quant Studio"
requires-python = ">=3.13"
dependencies = [
    "jsonschema==4.26.0",
    "rfc3339-validator==0.1.4",
    "starlette==1.6.0",
    "uvicorn==0.52.1",
]

[tool.uv]
package = false
